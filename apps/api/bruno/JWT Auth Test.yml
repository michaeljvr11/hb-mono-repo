info:
  name: JWT Auth Test
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseurl}}:{{hb_backend}}/auth/test"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
