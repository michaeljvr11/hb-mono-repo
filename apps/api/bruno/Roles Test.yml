info:
  name: Roles Test
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseurl}}:{{hb_backend}}/auth/vendor-test"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
