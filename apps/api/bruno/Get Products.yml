info:
  name: Get Products
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseurl}}:{{hb_backend}}/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
