info:
  name: Admin List Synonyms
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseurl}}:{{hb_backend}}/admin/search/synonyms"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
