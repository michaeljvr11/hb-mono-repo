info:
  name: Get Vendor Profile
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseurl}}:{{hb_backend}}/vendors/me"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
