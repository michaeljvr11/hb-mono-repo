{
  "name": "@hb/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts (DTO interfaces, enums) for HB web and api",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "watch": "tsc -p tsconfig.lib.json --watch"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
